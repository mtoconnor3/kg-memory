{
  "name": "kg-memory",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "pi": {
    "extensions": [
      "./index.ts"
    ]
  },
  "dependencies": {
    "@photostructure/sqlite-vec": "^1.1.1",
    "better-sqlite3": "^12.10.0"
  },
  "devDependencies": {
    "vitest": "^4.1.8"
  }
}
