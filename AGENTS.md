# KG Memory Extension — AGENTS.md

When working in this directory, **commit often**.

- Commit after every meaningful change (add node, fix search, add tool).
- Use descriptive messages: `feat: add kg_neighbors with BFS traversal`, not `fix stuff`.
- A clean git history makes debugging the graph and rolling back mistakes trivial.

This extension persists knowledge across all sessions. Treat it like a database — respect the schema, back up before experiments, and verify queries before trusting results.
